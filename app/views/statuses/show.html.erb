<%= link_to "Home", root_path %>

<% if notice %>

<p><%= notice  %></p>
<% end %>

<p> Username:
  <span>
    <%= @status.user_name %>
  </span>
</p>
<p> Status:
  <span>
    <%= @status.description %>
  </span>
  <span> <%= button_to 'Like', like_path(@status)  %>
</p>
<divclass="text-right">
  <%= link_to 'Edit', edit_status_path(@status)%>
  <%= link_to 'Delete', @status, class: "", method: :delete, data: { confirm: 'Are you sure?' } %>
</div>
